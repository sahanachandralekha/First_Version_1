import { Link } from "@tanstack/react-router";
import { ArrowLeft, Settings } from "lucide-react";
import { BottomNavigation } from "./BottomNavigation";
export function ScreenShell({ title, subtitle, children, nav = true }: { title: string; subtitle: string; children?: React.ReactNode; nav?: boolean }) {
  return <div className="min-h-dvh bg-canvas"><div className="mx-auto flex min-h-dvh max-w-xl flex-col bg-background shadow-inai"><header className="grid grid-cols-[3rem_1fr_3rem] items-center gap-2 border-b border-line px-4 py-3"><Link to="/home" aria-label="Back to home" className="grid size-12 place-items-center rounded-control text-foreground hover:bg-accent"><ArrowLeft aria-hidden="true"/></Link><div className="text-center"><p className="text-lg font-extrabold">{title}</p><p className="truncate text-xs text-muted-foreground">{subtitle}</p></div><Link to="/settings" aria-label="Open settings" className="grid size-12 place-items-center rounded-control text-foreground hover:bg-accent"><Settings aria-hidden="true"/></Link></header><div className="flex-1 px-5 py-8"><h1 className="text-3xl font-extrabold leading-tight">{title}</h1><p className="mt-2 text-base text-muted-foreground">{subtitle}</p>{children ?? <div className="mt-8 rounded-card border border-line bg-canvas p-6"><p className="font-bold text-primary">Foundation ready</p><p className="mt-2 text-sm text-muted-foreground">This screen is prepared for its reference-matched build stage.</p></div>}</div>{nav && <BottomNavigation />}</div></div>;
}
